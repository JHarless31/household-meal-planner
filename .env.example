# ============================================================================
# Household Meal Planning System - Environment Configuration
# ============================================================================
#
# This file contains all environment variables needed to run the application.
# Copy this file to .env and fill in your actual values.
#
# Quick Setup:
#   cp .env.example .env
#   nano .env  # Edit with your values
#
# Generate secrets with: openssl rand -hex 32
#
# ============================================================================

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# PostgreSQL database connection settings
# For Docker: Use service name as host (postgres)
# For local: Use localhost

DB_HOST=postgres
DB_PORT=5432
DB_NAME=household_db
DB_USER=household_app
DB_PASSWORD=<generate-secure-password>

# To generate a secure password:
# openssl rand -base64 32

# ----------------------------------------------------------------------------
# Authentication & Security
# ----------------------------------------------------------------------------
# JWT (JSON Web Token) configuration for API authentication

JWT_SECRET=<generate-secure-secret>
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Session secret for cookie-based authentication
SESSION_SECRET=<generate-secure-secret>

# To generate secrets:
# openssl rand -hex 32

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------

APP_NAME=Household Meal Planning System
APP_VERSION=1.0.0
ENVIRONMENT=production
DEBUG=False

# CORS (Cross-Origin Resource Sharing) allowed origins
# Comma-separated list of allowed origins
# Examples: http://localhost:3000,https://meal-planner.local
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Allowed hosts (for production, specify actual domains)
ALLOWED_HOSTS=*

# ----------------------------------------------------------------------------
# Recipe Scraping
# ----------------------------------------------------------------------------
# Configuration for web scraping recipes from external sites

SCRAPER_USER_AGENT=HouseholdMealPlanner/1.0 (+http://meal-planner.local/about)
SCRAPER_RATE_LIMIT=5
SCRAPER_TIMEOUT=10

# SCRAPER_RATE_LIMIT: Seconds to wait between requests to the same domain
# SCRAPER_TIMEOUT: Maximum seconds to wait for a response

# ----------------------------------------------------------------------------
# File Upload
# ----------------------------------------------------------------------------
# Configuration for recipe image uploads

MAX_FILE_SIZE=5242880
UPLOAD_PATH=/app/uploads

# MAX_FILE_SIZE: Maximum file size in bytes (5242880 = 5MB)
# UPLOAD_PATH: Directory path for storing uploaded files

# ----------------------------------------------------------------------------
# Pagination
# ----------------------------------------------------------------------------
# Default pagination settings for API list endpoints

DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# ----------------------------------------------------------------------------
# Frontend Configuration (Next.js)
# ----------------------------------------------------------------------------
# These variables are used by the frontend application

NEXT_PUBLIC_API_URL=http://localhost:8000
NODE_ENV=production

# For production deployment, set NEXT_PUBLIC_API_URL to your backend URL
# Example: NEXT_PUBLIC_API_URL=https://api.meal-planner.local

# ----------------------------------------------------------------------------
# Optional: Email Configuration
# ----------------------------------------------------------------------------
# Configure these if you want email notifications (future feature)

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM_EMAIL=noreply@meal-planner.local
# SMTP_FROM_NAME=Meal Planning System

# ----------------------------------------------------------------------------
# Optional: Backup Configuration
# ----------------------------------------------------------------------------
# Automatic backup settings

# BACKUP_PATH=/backups
# BACKUP_RETENTION_DAYS=30
# BACKUP_ENABLED=true

# ----------------------------------------------------------------------------
# Development Settings (Local Development Only)
# ----------------------------------------------------------------------------
# Uncomment these for local development

# DEBUG=True
# ENVIRONMENT=development
# DB_HOST=localhost
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# End of Configuration
# ============================================================================
